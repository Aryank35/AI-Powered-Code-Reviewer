import 'react';
import "./styles/Home.css"; // Use the same CSS file

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1 className="navbar-title">CodeAI</h1>
    </nav>
  );
};

export default Navbar;
